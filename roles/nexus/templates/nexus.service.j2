[Unit]
Description=nexus service
After=network.target

[Service]
Type=forking
ExecStart={{ nexus_install_latest }}/bin/nexus start
ExecStop={{ nexus_install_latest }}/bin/nexus stop
User={{ nexus_user }}
Restart=on-abort

[Install]
WantedBy=multi-user.target
